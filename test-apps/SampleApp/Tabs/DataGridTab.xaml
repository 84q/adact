<UserControl x:Class="SampleApp.Tabs.DataGridTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
    <StackPanel Margin="10">
        <TextBlock Text="Editable DataGrid: Grid/Table/GridItem/TableItem plus Value, Toggle, SelectionItem inside cells." FontWeight="Bold" />
        <DataGrid x:Name="MainDataGrid"
                  Height="180"
                  AutoGenerateColumns="False"
                  CanUserAddRows="True"
                  IsReadOnly="False"
                  SelectionUnit="FullRow"
                  SelectionMode="Extended"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  AutomationProperties.AutomationId="DataGrid_Grid_EditableMain"
                  AutomationProperties.Name="Editable Main Data Grid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="50" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="250" />
                <DataGridComboBoxColumn Header="Category" SelectedItemBinding="{Binding Category}" Width="120" x:Name="CategoryColumn" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price, StringFormat=C}" Width="100" />
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="80" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Text="Wide horizontally scrollable DataGrid: many columns for ScrollPattern horizontal values." Margin="0,15,0,5" FontWeight="Bold" />
        <DataGrid x:Name="WideDataGrid"
                  Height="150"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  HorizontalScrollBarVisibility="Auto"
                  VerticalScrollBarVisibility="Auto"
                  AutomationProperties.AutomationId="DataGrid_Grid_WideHorizontal"
                  AutomationProperties.Name="Wide Horizontal Scroll Data Grid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="160" />
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="260" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="140" />
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="120" />
                <DataGridTextColumn Header="Active" Binding="{Binding IsActive}" Width="100" />
                <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" Width="160" />
                <DataGridTextColumn Header="Region" Binding="{Binding Region}" Width="160" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="160" />
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="300" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock Text="Virtualized DataGrid: 1200 rows with row/cell virtualization and recycling." Margin="0,15,0,5" FontWeight="Bold" />
        <DataGrid x:Name="VirtualizedDataGrid"
                  Height="220"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  IsReadOnly="True"
                  EnableRowVirtualization="True"
                  EnableColumnVirtualization="True"
                  VirtualizingStackPanel.IsVirtualizing="True"
                  VirtualizingStackPanel.VirtualizationMode="Recycling"
                  ScrollViewer.CanContentScroll="True"
                  SelectionUnit="CellOrRowHeader"
                  SelectionMode="Extended"
                  AutomationProperties.AutomationId="DataGrid_Grid_VirtualizedLarge"
                  AutomationProperties.Name="Virtualized Large Data Grid">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Id" Binding="{Binding Id}" Width="60" />
                <DataGridTextColumn Header="Name" Binding="{Binding Name}" Width="150" />
                <DataGridTextColumn Header="Category" Binding="{Binding Category}" Width="120" />
                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="140" />
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="260" />
            </DataGrid.Columns>
        </DataGrid>
    </StackPanel>
    </ScrollViewer>
</UserControl>
